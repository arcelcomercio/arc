const StringImageItem = ({siteDomain='',siteUrl=''}) => {
  return `
  <url>https://publimetro.pe/f/i/pub_40.png</url>
  <title>${siteDomain}</title>
  <link>${siteUrl}</link>
  `
}

export default StringImageItem
